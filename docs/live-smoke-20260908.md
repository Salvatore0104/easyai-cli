# 2026-09-08 CLI 真实生成实测

使用账户API Key经本地CLI提交1张GPT Image 2图片和1段MiniMax-H3视频，均为纯文本。首轮未测Seedance或参考素材模式，后续单图全能参考结果见下文。

图片请求1K、16:9，提交等待与下载成功；实际1672×941。舞台构图、晶体和留空可辨，整体偏暗。

H3请求4秒、720p、16:9、audio:true、watermark:false。第一次因缺少content字段收到HTTP400，没有任务ID。依据网站已有成功契约，将CLI的prompt映射为content中的text，保持创意和计费设置不变。修正请求生成成功：1344×768、24fps、容器4.458秒，AAC 32kHz双声道。输出与标称比例／时长不完全一致，不静默裁切。

该视频经历60秒POST超时，初始恢复窗口未找到任务；稍后tasks resume凭同一幂等键和唯一新增视频记录恢复成功，再用video download取回结果，没有重复生成。后续H3提交等待已调整为300秒，保留防重复与查询恢复；后续单图全能参考请求已验证该等待窗口，生成与下载正常完成。

本次发现并修复：H3文本content适配；400参数拒绝记录为rejected且禁止错误恢复；H3慢接收超时窗口。补充对应回归测试。后续追加图片全能参考适配与实测，未验证混合音视频参考。

两项任务的status/download/resume响应都未返回实际积分字段，pointsUsage如实为not_reported；未用余额差估计实扣。平台需要补充任务级结算字段才能可靠报告费用。

检查了图片、视频接触表、尾帧及8个右下角采样，未见明显水印；采样中可见褶皱展开和圆环扩张，视频造型较简单。未做主观音频试听、逐帧或现场LED验收。


## H3 单图全能参考追加验收

按用户要求使用上轮GPT Image 2舞台图作为唯一参考，经CLI提交一次 `mode:image_reference`，转换为 `videoGenerateMode:omni_reference` 及有序 `content`，每个图片项角色为 `reference_image`。请求4秒、720p、16:9、audio:true、watermark:false；1图、0视频、0音频参考。使用官方Ref2VA六段结构，普通设计参考而非严格首帧。

任务 `6a9fa8f97e18d7bc65200820` 成功，CLI在300秒提交窗口内返回并自动下载，没有重试、重复任务或手工恢复。实际1344×768、24fps，容器时长4.458秒（标称4秒），音轨AAC、32kHz双声道。平台仍未返回本任务实际积分，不能报告0积分或用余额差推算。

实际查看原图、8帧接触表、尾帧及8个右下角裁切。黑色环形结构、红色晶体、蓝色中轴及下方暗色留空稳定保留；晶体变化幅度偏小，不能夸大为强烈动作编排。采样未发现明显可见水印。接口及单图参考保真验收通过；最终演出节奏、逐帧、主观音频和现场LED未验收。无自动重生成。

新增回归覆盖图片顺序、原生content冲突、原生模式冲突、空参考、重复URL导致上游去重、文本模式偷带参考和未验证混合素材拒绝。完整72项测试、类型检查、构建、资源检查及Skill校验通过。媒体与包含请求明细的账本保存在仓库外，未提交凭据和个人输出。
